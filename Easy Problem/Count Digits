/*
Count Digits

Difficulty: Easy Accuracy: 30.45% Submissions: 447K+ Points: 2

Given a positive integer n, count the number of digits in n that divide n evenly (i.e., without leaving a remainder). Return the total number of such digits.

A digit d of n divides n evenly if the remainder when n is divided by d is 0 (n % d == 0).
Digits of n should be checked individually. If a digit is 0, it should be ignored because division by 0 is undefined.

problem link : https://www.geeksforgeeks.org/problems/count-digits5716/1?page=1&difficulty=Basic,Easy&status=solved&sortBy=submissions

*/
class Solution {
    static int evenlyDivides(int n) {
        
       int digit,count=0;
       int dup=n;
      
        while(n>0)
        {
         digit=n%10;
         if(digit!=0 && dup%digit==0)
         {
             count++;
         }
         n=n/10;
        }
        return count;
    }
}

/*Note:
    **  Declare Global var to store last digit and increase count
    **  store n as duplicate in dup var
    ** when number is  > 0 enters the loop
    ** to get last digit --n % 10 used
    ** check when digit not equal to 0 && dup (n) divide digit leaves remainder 0
    ** then increase count by 1
    ** then to get next digit by change decimal use ==n / 10
    ** return count (total digits is returned in count)
    */


















class Solution {
    static int evenlyDivides(int n) {
        
       int digit,count=0;
       int dup=n;
      
        while(n>0)
        {
         digit=n%10;
         if(digit!=0 && dup%digit==0)
         {
             count++;
         }
         n=n/10;
        }
        return count;
    }
}

/*Note:
    **  Declare Global var to store last digit and increase count
    **  store n as duplicate in dup var
    ** when number is  > 0 enters the loop
    ** to get last digit --n % 10 used
    ** check when digit not equal to 0 && dup (n) divide digit leaves remainder 0
    ** then increase count by 1
    ** then to get next digit by change decimal use ==n / 10
    ** return count (total digits is returned in count)
    */
